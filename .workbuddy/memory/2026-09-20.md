# 2026-09-20 工作日志

## 提醒强度等级（黄/红）—— 两套提醒统一口径

**用户拍板（回应 docs/47 §6 待办 #4）**
- `thin_depth` 的触发判据 **保留"首档为基准"，不改 ≤5bp**。理由：该判据只决定"要不要提醒"，
  改成 ≤5bp 会削弱提醒强度；用户倾向"提醒宁可早"。
- 新增需求：提醒要有**强度等级**，且**两套提醒统一**；明确**不要绿**（无提醒不占色位）。

**实现（只加字段，未改任何判断/阈值/项目结构）**
- 新增 `common/alert_level.py`：统一强度单一实现。`YELLOW`/`RED` 两档，
  `from_risk_level()`（caution→黄、veto→红）、`from_alert_level()`（warn→黄、critical→红、info→无）、
  `worst()` 取最严重、`tag()` 控制台标签。附 `selftest()`（6 项）。
  放 `common/` 而非 `project2/`，遵循"删掉 project2/ 照样完整运行"的硬约束。
- `project2/agent_team.py`：在规则表的 for 循环里给每条规则加 `intensity`/`intensity_label`；
  风控返回体加 `intensity`（本次所有触发里最严重的）与 `intensity_label`；
  `render_risk` 只有**触发**的规则与结论行才显示强度。
- `tools/position_watch.py`：`_alert()` 每条加 `intensity`/`intensity_label`；
  `run_once` 输出加 `intensity_counts` 与整体 `intensity`（弹窗可直接读）；
  CLI 打印新增"提醒强度（黄/红，无绿）"一行 + 每条告警后缀；`selftest` 加 3 项。
- 文档：`docs/47` §6 #4 标记为已确认，新增 §8 记录口径与验证；`project2/README.md` 规则表下加说明。

**验证（系统 Python 3.14，`C:\Users\32041\AppData\Local\Programs\Python\Python314\python.exe`）**
- `python common\alert_level.py` → 6/6 通过
- `python tools\position_watch.py --selftest` → 9/9 通过（含"没有绿"）
- `python project2\agent_team.py --decision-selftest` → 全通过（含 thin_depth 5 条边界）
- `python project2\agent_team.py --selfcheck` → 全部自检通过
- `python tools\reproduce_check.py` → 103 通过 / 0 警告 / 1 失败

**环境坑（非本次改动引起）**
- 托管 Python 3.13.12 缺 `tzdata`，`ZoneInfo("America/New_York")` 报错 → 用系统 Python 跑。
- `execution_cost.py --selftest` 最后一步 `os.remove` 被本机沙箱的 safe-delete 拦截
  （`windows-sandbox-recycle-bin-unavailable`），导致 reproduce_check 唯一 1 项失败；
  缓存文件未丢（`data/derived/joint_fill_all.csv` 仍在）。

---

## 二、把强度等级接到前端：右下角提醒弹窗

**用户指令**：「接上」——把上一节的 `intensity` 字段接到页面上（用户 09-18 就要求过"右下角闪烁弹窗"）。

**改动**
- `server/app.py`：新增 `ALERT_FILE` 常量 + `build_alerts()`，注册路由 `/api/alerts`。
  **只读** `data/positions/alerts.json`（容忍 utf-8-sig/utf-8/gbk）；老文件缺 `intensity`
  时按 `level` 兜底现算；文件缺失 → `available:false`（不假装健康）；告警按强度排序。
- `web/index.html`：`</main>` 后新增 `<aside id="alert-dock">`（head/list/foot + 收起按钮）。
- `web/styles.css`：`.alert-dock`（fixed 右下角）+ `lvl-yellow` / `lvl-red`
  + `alert-pulse` 动画（1.7s 呼吸）+ `prefers-reduced-motion` 退化为静态描边。
- `web/app.js`：`mdInline()`（先 esc 再渲染 `**粗体**`/反引号，避免页面出现字面符号）、
  `fmtAgo()`、`loadAlerts()`；`boot()` 里首屏 + 每 20s 刷新。
  收起按**批次时间戳**记账，只有新一批才再弹。
- `tools/reproduce_check.py`：must 清单加 4 条源码特征（/api/alerts、alert_level、
  position_watch intensity、app.js loadAlerts）+ `alert_level.py` 行为自检。

**验证（Chrome headless 真浏览器 + 自写探针）**
- 有告警：`class=alert-dock lvl-red`、`display:flex`、高 387px、0 字面 `**`、0 console 报错
- 无告警（指向不存在的告警文件）：`hidden` / `display:none` / 高 0、页面无"一切正常"字样
- 点「收起」：收起成功、0 报错
- `tools/ui_check.py` 通过；`reproduce_check.py` → 108 通过 / 1 失败（仍是沙箱回收站那条）

**环境坑（新踩）**
- 本机 8787/8789 端口上有其它服务（8789 上是一个叫 `execution-aware-alpha` 的应用，
  它也有 `/api/alerts`，schema 是 `{ok,alerts,exists,note}`，容易和本项目混淆）。
  Windows 的 `SO_REUSEADDR` 允许多个进程绑同一端口且**不报错**，
  于是"自己的服务打印了启动横幅"也可能读到别人的响应 —— 验证多实例场景时务必换空闲端口。
- 本机 bash 没有 `sleep`，等待请用 python 轮询或 PowerShell。

