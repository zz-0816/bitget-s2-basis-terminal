# 窗口预检报告

- 生成时间：2026-09-13 15:10:30（UTC+8）
- 当前 route：`in_house`
- 当前 session：`closed`
- **已在窗口内**，距关闭 16.8 小时
- 距**下一个**窗口开启 136.8 小时（09-19 08:00）

## 1. 采样器实例校验

| 采样器 | 实例数 | 锁持有者 | 持锁者存活 | 判定 |
|---|---|---|---|---|
| `spread_sampler` | 1 | 25512 | 是 | OK |
| `sampler_universe` | 1 | 1312 | 是 | OK |
| `orderbook_sampler` | 1 | 14372 | 是 | OK |

## 2. 守护进程

- `sampler_supervisor` 实例数：**1**（持锁者 26172）
- 判定：OK

## 3. K 线新鲜度（可回补）

| 粒度 | 滞后 | 判定 |
|---|---|---|
| `1m` | 816 分钟 | **过旧** |
| `1h` | 816 分钟 | **过旧** |
| `1D` | 816 分钟 | **过旧** |

## 4. 时间预算

- 已在窗口内；本次预检针对**下一个**窗口。
- 下一个窗口开启：09-19 08:00（距今 136.8 小时）
- 预检建议在开启前 60 分钟执行；若已是窗口内，则预检只为**下一个**窗口做准备。
- **窗口开启后无法追补**：盘口/深度数据交易所不留存，错过即永久丢失。

## 结论

**全部检查通过** —— 采样器各 1 实例、守护在位、K 线新鲜。

> 本脚本**不 kill、不重启任何采样器**，只做只读检查（+ 可选补 K 线），
> 以免『检查动作本身』造成中断。