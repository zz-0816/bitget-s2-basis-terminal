# 窗口预检报告

- 生成时间：2026-09-15 22:37:06（UTC+8）
- 当前 route：`stockroute`
- 当前 session：`intraday`
- 距下一个窗口开启 81.4 小时（09-19 08:00）

## 1. 采样器实例校验

| 采样器 | 实例数 | 锁持有者 | 持锁者存活 | 判定 |
|---|---|---|---|---|
| `spread_sampler` | 1 | 25512 | 是 | OK |
| `sampler_universe` | 1 | 1312 | 是 | OK |
| `orderbook_sampler` | 1 | 14372 | 是 | OK |
| `trades_sampler` | 1 | 12108 | 是 | OK |

## 2. 守护进程

- `sampler_supervisor` 实例数：**1**（持锁者 28272）
- 判定：OK

## 3. K 线新鲜度（可回补）

| 粒度 | 滞后 | 判定 |
|---|---|---|
| `1m` | 47 分钟 | OK |
| `1h` | 46 分钟 | OK |
| `1D` | 46 分钟 | OK |

## 4. 时间预算

- 距窗口开启 81.4 小时（4883 分钟）
- 预检建议在开启前 60 分钟执行；若已是窗口内，则预检只为**下一个**窗口做准备。
- **窗口开启后无法追补**：盘口/深度数据交易所不留存，错过即永久丢失。

## 5. 事件闸门体检（窗口内唯一可挂单，必须先确认闸门可用）

- 闸门模块可导入并调用（`project2/event_gate.static_gate`）
- ✅ 当前没有标的被闸门否决。
- 人工日历：7 条财报（其中已复核 0 条）+ 7 条宏观
- 财报覆盖到 2026-12-03（还有 79 天）
- 宏观覆盖到 2026-11-12（还有 58 天）
- 🔴 一条财报都未经复核 —— 当前闸门只能挡住『可计算事件』，挡不住财报。这是已知缺口。
- 可计算层（opex / triple witching / 休市日）：永不过期，由 `market_events.py` 按规则算出

> 闸门是否决权：`block` 时挂单类方案作废（见 `project2/README.md` §4.1）。
> 闸门不可用时执行模型会降级为 `caution`，**不会静默放过**。

## 结论

**全部检查通过** —— 采样器各 1 实例、守护在位、K 线新鲜、事件闸门可用。

> 本脚本**不 kill、不重启任何采样器**，只做只读检查（+ 可选补 K 线），
> 以免『检查动作本身』造成中断。