# 提交材料集中整理（填表时照抄）

> 用途：Google Form 要填链接时**不用现找**。
> 负责人 **A** ｜ 最后更新 **2026-09-18**（原 09-14 版已过期，见 §0 变更）
> ⚠️ 填表前逐项点开确认可访问（尤其是仓库要**公开**）。
> 🔴 **两投的分工与统一流程见 `docs/31-两投材料与统一流程.md`**（含七个交接点）

---

## 0. 相对 09-14 版的变更（数字都变了，别照旧版抄）

| 项 | 旧 | 新 |
|---|---|---|
| 一键自检项数 | 49 项 | **87 项通过 / 0 警告 / 0 失败** |
| 数据样本 | 872 个 gz + `MANIFEST.md` | 另加 **`data/samples/MANIFEST-B.md`**（62 个文件 / 603.7 MB，含**对应任务**与 SHA256） |
| 数据字典陷阱 | 15 条 | **19 条**（新增：现货成交带只到 09-14、两个成交率分母不同、分层统计双计、分层键当 base） |
| 第二项目状态 | 「次选，时间不足可放弃」 | ✅ **已完成到可交付**（多 Agent 四层 + 联合分布 + 可复跑日志） |
| 新增局限 | — | **现货腿自 09-14 起零成交**（`docs/29`）、联合分布只覆盖三天 |
| 新增演示入口 | — | `python tools\unified_demo.py --base NVDA`（一条链跑完两个项目） |

---

## 1. 核心链接

| 材料 | 链接 | 状态 |
|---|---|---|
| **GitHub 仓库（代码）** | https://github.com/zz-0816/bitget-s2-basis-terminal | ✅ 已公开 |
| **本机 Demo（监控台）** | http://127.0.0.1:8787 | ⚠️ 仅本机；录屏或截图代替（09-18 实测 `/api/health` 200） |
| **统一流程演示（命令行）** | `python tools\unified_demo.py --base NVDA` | ✅ 一行跑完两个项目 |
| **一键复跑自检** | 仓库内 `python tools\reproduce_check.py`（**87 项**，约 60 秒） | ✅ |
| **数据样本与索引** | `data/samples/`（872 个 gz + `MANIFEST.md`）+ **`data/samples/MANIFEST-B.md`** | ✅ |
| **回测 / 分析结论** | `docs/13`（净收益判据）、`docs/14`（摩擦预算 + §9 联合分布）、`docs/15`（route 对照） | ✅ |
| **数据字典** | `docs/DATA_DICT.md`（口径唯一来源 + **19 条陷阱**） | ✅ |
| **两投分工与串联** | **`docs/31-两投材料与统一流程.md`** | ✅ 09-18 新增 |

---

## 2. 报告要用的"一句话 + 三个数字"

**一句话**：Bitget rToken 现货与美股 USDT-M 永续之间的跨场所价差，
在**平台所内撮合窗口**（周末/节假日）才具有零售可执行性。

**三个数字**（全部可复跑）：

| 数字 | 值 | 来源 |
|---|---|---|
| 所内撮合 vs 直连的现货点差 | **7.34 bp vs 3.61 bp（2.03 倍）** | `docs/15` §2.1 |
| 越过成本门槛的时间占比 | **38.4% vs 13.2%**（门槛 11.34 bp，已扣资金费） | `docs/15` §2.2 |
| 往返成本门槛 | **11.34 bp**（手续费 13.70 − 资金费收入 2.36） | `docs/14` §4 / §4.1 |

---

## 3. 「大模型在项目中的作用」独立字段（表单要单填）

照实写，别吹：

| 环节 | 大模型做了什么 | 可验证产出 |
|---|---|---|
| 主题筛选 | 从开发者手册里抽出子主题、比对"折溢价路线已被否证"后改选套利路线 | `docs/00-主题定案建议.md` |
| 口径纠错 | 发现并纠正基差符号、两个时段口径（`session` vs `route`）不能混用 | `docs/DATA_DICT.md`、`common/market_calendar.py` |
| 数据陷阱排查 | 定位 `rstrip("USDT")` 字符集 bug、上市前同名资产污染（剔除 59,849 根）、死报价 | `docs/TASKS.md` 阶段 1 |
| 统计结论收紧 | 主动用逐笔成交**推翻自己**的乐观结论（4 个通过标的 → 1 个） | `docs/14` |
| 工程与自检 | 采样守护、一键复跑自检、控制台编码兜底、后端提速 | `tools/reproduce_check.py`、`server/app.py` |

> **Alpha 来源说明必须包含这句**（`docs/TASKS.md` 阶段 4 的硬性要求）：
> 「我们实测发现 taker 路线被点差吃穿，据此转向 maker」——
> 实测依据：往返 taker 手续费 22 bp + 两腿点差，而现货中位点差仅 7.34 bp。
> 见 `docs/14` §3 / §4。

---

## 4. 项目说明 6 段（表单要求 6 段，第 2 段有硬性措辞要求）

| 段 | 内容 | 现成材料 |
|---|---|---|
| 1 | 问题：跨场所价差在哪里才真的可执行 | `docs/15` §0 |
| 2 | **目标用户**（⚠️ 必须用具体画像，**禁止写"所有 trader"**） | `docs/07` §1 |
| 3 | 数据：为什么必须自采（盘口无历史接口） | `docs/DATA_DICT.md` §1 |
| 4 | 方法：收益恒等式 + 双档检验 + 逐笔成交判定 | `docs/14` §2 |
| 5 | 结果：门槛 11.34 bp + 越线率 38.4% vs 13.2% | `docs/14` §4、`docs/15` §2 |
| 6 | **局限**（必须主动写） | 见下面 §5 |

---

## 5. 必须主动写明的局限（评委最看重这个）

| # | 局限 | 出处 |
|---|---|---|
| 1 | **永续 1H 历史仅 58 天**（平台限制），样本跨度受限 | `docs/TASKS.md` 阶段 1 |
| 2 | **永续深度/容量约束**：现货腿才是瓶颈（成交额仅为永续的 1/20 ~ 1/300） | `docs/14` §3.2 |
| 3 | **所内窗口内有 13–17 小时不可回补的采样缺口**（采样器启动晚于窗口开启） | `docs/TASKS.md` 阶段 1 |
| 4 | **样本内结果，非样本外**（样本外由 B 在 09-17 前完成） | `docs/TASKS.md` 阶段 3 |
| 5 | **资金费 74.2% 结算点为 0**，收入是期望值而非稳定现金流 | `docs/14` 局限 8 |
| 6 | **开放问题**：单次快照里永续 `indexPrice` 与同期原生股价差约 2.8%，需指数历史序列才能判 | `docs/14` §4.2 |
| 7 | 🔴 **现货腿自 09-14 起零成交**（上游报价仍在，成交为 0）→ "现货挂单赚点差"这条路径**当前不可执行** | `docs/29` |
| 8 | **联合分布只覆盖 09-12~09-14**（现货成交带的有效期），且只是**分布**不是逐笔 | `docs/14` §9.4 |
| 9 | **采样缺口合计约 9.3 小时**：8.04 小时（09-16/17，100% `stockroute`）+ 74.9 分钟（09-18，同样 100% `stockroute/premarket`，`in_house` 0 分钟） | `docs/27`、`docs/30` §3 |

> ✅ **曾有的一条候选局限已被我自己否掉并撤回**：
> "rToken 跟踪偏离 −33 ~ +85 bp" 是**方法错误**（跨时点相减）。
> 用 Yahoo 原生股价做外生参照、严格同刻对齐后，实测中位 **+1.7 bp**、区间 −7.1 ~ +12.0 bp。
> **报告里不要再写这条"风险"** —— 主动撤回错误结论本身就是可信度证据，
> 可以在报告里作为"我们如何自查"的一例（见 `docs/14` §4.2）。

---

## 6. 表单勾选与补贴（逐项确认）

| 项 | 建议 | 依据 | 状态 |
|---|---|---|---|
| **Demo Day** | **建议勾选** | 有可运行成品（监控台 + 自检脚本） | ⬜ 待勾 |
| **K3 补贴（30U）** | 按需 | 需完成 KYC | ⬜ 待办 |
| **高校名称** | 如适用则填 | 手册未强制 | ⬜ 待确认 |
| **Qwen 额度独立表单** | **前 300 队 30U，需 KYC** | 与主表单**分开填** | ⬜ 待办 |
| **两投（两个项目）** | 分**两次**填表 | 手册要求独立项目 | ⬜ 见 §7 |

---

## 7. 两投的第二项目（**已完成到可交付**，不再是"次选"）

> 🔴 手册原文：「一支队伍最多向 2 个不同主题提交，**每个主题须是独立项目，分两次填表**。」
> **不能合成一项提交**；但**交互可以是同一条流程** —— 完整说明见
> **`docs/31-两投材料与统一流程.md`**（含七个交接点与两份材料清单）。

| 项 | 内容 |
|---|---|
| 定位 | 开放主题「**Execution-aware Alpha**」——点差/成本感知的执行层 |
| 主题 | 与项目一**不同**的开放主题（不是同题拆两半） |
| 复用 | 既有采样数据 + 成本模型（**不重复造轮子**），只读引用、反向依赖为零 |
| 已交付 | ① 双腿成本模型 ② 多 Agent 四层（分析师/辩论/交易员/风控官）③ 可复跑辩论日志 |
| 关键实测 | 门槛 11.34 bp｜`P(只成交一腿)=24.9%` vs `P(两腿都成交)=0.70%`（旧口径高估 5~18 倍） |
| 自检 | `python project2\agent_team.py --selfcheck`（四层全过，无需网络） |
| 独立仓库 | 需独立；材料分开指向自己的入口 |

---

## 8. 提交前 10 分钟终检

```powershell
# 1) 一键复跑自检（退出码 0 = 通过）—— 87 项
python tools\reproduce_check.py

# 2) 统一流程演示（一条链跑完两个项目，截图/录屏素材）
python tools\unified_demo.py --base NVDA

# 3) 采样器健康（受限会话会自动降级为新鲜度判据，不误报）
python tools\check_samplers.py --json

# 4) 口径回归（基差符号不能被改坏）
python tools\verify_basis_convention.py

# 5) 监控台可用
python server\app.py --port 8787
```

- [ ] 仓库**已公开**（不是 private）
- [ ] README 的"快速开始"能照做（已由自检第 5 层保证链接有效）
- [ ] `docs/TASKS.md` 的 🔴 硬门禁全部勾选
- [ ] **两投各填一次表**，各自指向自己的入口（`docs/31` §3）
- [ ] 每份材料里都写了**与另一份的接口**（七个交接点）
- [ ] **X 帖已发**（含 `#BitgetHackathon` + `@Bitget_AI` + 转发官方帖）→ `docs/16`
- [ ] 报告里**没有**"无风险套利"字样，统一用「做市型价差捕获」
- [ ] 报告里写明"**代币 ≠ 股权**"与"非投资建议"
