# 28 · 给乙侧的任务清单（09-17 更新版）

> 负责人 **A** ｜ 生成 **2026-09-17** ｜ 截止 **09-21**
> 本文**取代** `docs/21-给乙侧的任务清单.md` 里未完成的条目，是乙侧**当前**的完整清单。
> 配套：`docs/12`（10 分钟定位）、`docs/18`（可直接粘进 6 段表单）

---

## 0. 本次更新的前提变更

**A 侧决定：乙侧不再持续采样，数据只用 A 侧这一台机器。**

这条变更有两个后果，都必须如实处理，不能只改任务不改说法：

| 后果 | 处理 |
|---|---|
| ✅ **已完成的**那次双机对照仍然有效 | 09-12 做过一次，572 组重叠三元组、中间价相对差中位 **0.00 bp**，数据已归档、脚本可复跑。**这是既成事实，不因停止采样而失效。** |
| ⚠️ **未来的**交叉验证没有了 | 提交材料里"两台机器**独立采样互为验证**"是**现在进行时**的表述。**A 已于 09-17 全部改为"曾做过一次一致性对照"**，见 T5。 |
| ⚠️ T2 的"独立性"要降级 | 乙不再有独立数据源，只能从**同一份原始数据**复算。这仍然是复核，但**不是独立验证** —— 措辞必须区分。 |

---

## 🔴 T0：A 违反了自立的冻结边界（流程问题，需乙记录）

`docs/21` 声明过「**09-16 之后 A 不改冻结清单里的任何文件**」。A 在 **09-17 02:45** 改了其中一个：

| 文件 | 状态 |
|---|---|
| `tools/reproduce_check.py` | ❌ **被 A 改动**（自检 83 → 87） |
| `common/market_calendar.py` | ✅ 未动（09-13） |
| `build_panel.py` | ✅ 未动（09-14） |
| `data/panel/1h_10pairs.csv` | ✅ 未动（09-14） |
| `data/derived/*.csv`（18 个） | ✅ 全部未动（09-12 ~ 09-14） |

**改动性质**：只**新增断言**（辩论层自检 + 3 项源码特征检查），
**没有改动任何数字、任何结论、任何被复算的量**。但文件哈希确实变了。

**请乙**：冻结时用当前哈希，并在 `frozen.json` 里加 `amended` 记录，
写清文件名、改动时间、改动性质，以及"结论未变"的依据 ——
依据可直接复跑：`python tools/reproduce_check.py` 由 83 通过变 87 通过，
**原 83 项一项没少、没有任何一项从通过变成失败**。

---

## 🔴 T1：参数冻结 `config/frozen.json`（原 B1，**已逾期**，截止 09-16）

A 实测：**`config/frozen.json` 不存在**，这条硬门禁还没做。

冻结内容（`docs/21` §「冻结前必须知道的事」已列全）：
`common/market_calendar.py`、`build_panel.py`、`data/panel/1h_10pairs.csv`、
`data/derived/*.csv`、`tools/reproduce_check.py`（连同 T0 的 `amended` 记录），
外加 **git tag + 快照 SHA256 + 时间戳**。

---

## 🔴 T2：复核 8 小时断口（**口径已降级，见下**）

A 在 `docs/27` 给出：断口 8.04 小时、净损失约 **298,700 行**、占全库 **8.2%**，
并称该窗口 **100% 是 `stockroute`、`in_house` 分钟数为 0**。

**请乙复核这四条**：

1. 断口起止时刻是否一致（甲侧 18:19:18~18:19:56 断、02:19:44~02:22:00 恢复）；
2. 丢失量口径是否合理（A 用断流前 2 小时健康窗口的**实测速率**折算，非估算）；
3. **是否有 A 漏掉的其他断口**；
4. ⭐ **`in_house` 分钟数为 0 是否成立** ——
   它是"影响不大"的**唯一支柱**，这条错了整段结论要推翻。

⚠️ **措辞要求**：乙不再有独立数据源，这次是**从同一份原始数据复算**。
报告里**不得**写成"经独立数据源验证"，只能写"经独立复算核对"。

⚠️ A 的工具踩过三个定位坑（已写进注释，乙复核时注意）：
- 用"最后一条样本"当断流起点：错。封锁是**概率性**的，偶有单条漏过；
- 用分位数：错。密集数据持续到断点，分位会切掉最后 2% 的正常数据；
- 只找"最后一个大间隙"：错。尾部开口断口后面没有"下一条"，会回溯到几天前的旧断口。

---

## 🔴 T3：滚动 30 天 Sharpe 曲线（**手册点名的审查重点，仍未交付**）

乙已交付单点 Sharpe（3.64）与 IS/OOS 切分（4.23 / 3.79，decay 0.896）。
但单点 Sharpe 证明不了稳定性。**请补**：
- 滚动 30 天 Sharpe 曲线（conditionally 加 rolling Sortino / MaxDD）；
- 回答两个问题：**曲线有没有区间跌破 1.0**？**衰减是渐变还是某一段突变**？

---

## 🔴 T4：复算成本阈值 **11.34 bp**（不许引用 A 的 derived CSV）

这是整个结论所系的**单一数字**：全挂单往返 14.0 bp / 永续吃单 22.0 bp，
扣掉资金费收入后降到 11.34 bp。

请从**原始费率**（`docs/09`）与**资金费口径**（`docs/14` §4.1）独立复算一遍。
对不上就报对不上 —— 这条对上了，容量与收益结论才有意义。

---

## 🔴 T5：修正"双机交叉验证"表述（**A 已改完，请乙复核**）

乙侧停止采样后，下列位置的说法必须改成**过去时**，否则就是**不实陈述**。
⚠️ **A 已于 09-17 全部改完**，乙的任务变成**复核有无遗漏**：

| 文件 | 位置 | 已改为 |
|---|---|---|
| `docs/24-项目说明成稿` | L233 | 「**曾用**两台机器独立采样做过一次一致性对照（09-12，572 组重叠三元组，中位差 0.00 bp）；此后由单机持续采样」 |
| `docs/24` | L291 | 命令行注释 → 「双机一致性对照（09-12 一次性）」 |
| `docs/18-A侧报告素材` | L88 | 「双机一致性对照（**一次性**，09-12）」 |
| `docs/22-A侧报告成稿` | L312 | 命令行注释 → 「双机一致性对照（09-12 一次性）」 |
| `docs/20-数据来源与清单` | L32 | 加注「**09-12 一次性双机对照**；乙侧此后未持续采样」 |
| `docs/20` | L143 | 命令行注释 → 同上 |
| `docs/13-基差分解` | L188 | 表格项 → 「双机一致性对照（09-12 一次性）」 |
| `README.md` | L206 | 「**历史资产**：09-12 一次性双机对照」 |

**未改**（它们描述的就是 09-12 那次事件本身，文件名与标题准确）：
`docs/b-side/双机交叉验证-20260912.md`、`docs/b-side/README.md`。

**为什么必须改**：手册明确「缺失/不实材料直接判无效」。
而真实情况**依然是个优点**（一次性对照做出 0.00 bp 中位差，说明两套实现口径一致），
只是**时态不能写成持续**。改完反而更站得住。

**请乙复核**：全部 markdown 里再搜一遍 `双机交叉验证` / `独立采样互为验证`，
确认除历史记录外没有残留的现在进行时表述。

---

## 📅 T6：09-19 08:00 → 09-21 08:00 `in_house` 窗口

这是**唯一能拿到干净周末点差的窗口**，且**窗口关闭日就是提交截止日**，错过没有第二次。

乙侧要做：
1. 窗口数据落地后的**复核清单**（点差中位、超阈值比例、深度分布）；
2. ⚠️ **代理单点依赖**：A 侧采样器现在**走代理**（事故前是直连）。
   代理一断，采样立刻再停。**该窗口期间代理必须全程在线**。
3. ⚠️ **不可回补**：盘口与点差是**时点快照**，停了永久丢失
   （只有成交可部分回补，交易所保留近期成交）。这个窗口没有补救机会。

---

## 📋 其余仍未完成的原清单项

| # | 任务 | 截止 | 备注 |
|---|---|---|---|
| B3 | 门禁自证：总期 ≥60 天 / 样本外 ≥30 天 / 衰减比 ≥0.5 | 09-17 | `python tools/sample_adequacy.py` |
| B4 | 回测**或**「做市 + 高/低波动连续记录」二选一，并声明走哪条 | 09-17 | 建议走回测 |
| B5 | 免责与术语合规：**做市型价差捕获（非无风险套利）**、**代币 ≠ 股权** | 09-20 | `docs/16` §5 |
| B6 | 项目说明 **6 段**（第 2 段禁「所有 trader」） | 09-20 | `docs/18` |
| B7 | 「大模型在项目中的作用」独立字段 | 09-20 | `docs/18` 附录 + `project2/event_gate.py` |

⚠️ 两条**不要写**进报告（会变成硬伤）：
- 曾声称「rToken 跟踪偏离 −33~+85 bp」—— **已撤回**，实测中位 +1.7 bp（`docs/14` §4.2）；
- 「用周末样本跑了 60 天回测」—— **两条轴必须分开声明**（`docs/21` 已写明）。

---

## 优先级

**T1 冻结（硬门禁，已逾期）> T5 复核表述（A 已改完，只需复核）> T6 窗口准备 > T3 滚动 Sharpe > T4 复算阈值 > T2 复核断口 > T0 记录**

T1 是**提交有效性**问题（不做即无效）；T5 是**不实陈述**风险（A 已消除，剩复核）；
T3 与 T4 是**结论可信度**问题。
