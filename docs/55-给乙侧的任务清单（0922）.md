# 55 · 给乙侧的任务清单（0922）

> 起因：甲方 2026-09-22 15:39 做了一次采样体检 + 缺口对照（结论见 §0），
> 由此得出乙侧需要做的 5 件事，按价值排序。**第 2、3 条最要紧。**
> 配套核对工具在甲方仓库：`tools/audit_manifest_b.py`、`tools/b_side_crosscheck.py`、
> `tools/gap_capacity_report.py`。

---

## 0. 甲方体检结论（2026-09-22 15:39 北京）

- **甲方 4 个采样器全部健康**：实例各 1、锁持有者正常、实时写入（15:39:39）。
- 当日累积：最优一档 18,253 行 / 5 档 332,740 / 逐笔 231,834 / 全池 85,639。
- 甲方自查发现 **K 线累积器滞后 47.5 小时**，已自行补齐
  （新增 830,706 根 / 1,823 页 / 0 错误 / 319 秒；复查滞后 1m=3 分 / 1D=0 分）。
  ——**这件事与乙侧无关，不用管。**

---

## T1（先回话）：你的采样器还活着吗？

**✅ 已由数据本身回答（2026-09-22 15:5x 甲方核查）：**

- 乙侧已于 09-21 13:28 推送了 `2026-09-21.csv`（15,415 行 / 771 轮），
  覆盖 **09-21 00:00:31 → 13:27:01（北京，共 14 小时）**，
  **13:27:01 之后就没有了** —— 即**采样器在 09-21 13:27 停了，至今约 50 小时**。
- 该增量已验收：**09-21 双机对照 15,276 组重叠三元组，中间价与点差中位差均为 0.00 bp**
  （最大 60.98 bp 出现在薄盘口的 SOXL，属快行情正常抖动）→ 同源可信。
- 09-21 的增量对"补缺口"贡献为 0（那天甲方全程在线，乙侧轮次全部被覆盖）；
  它的价值是**独立来源交叉复核**。
- 真正的新增来自更早的 W2：补充文件 `data/derived/bside_supplement_topofbook.csv`
  现 **16,925 行**，其中**甲方完全没有的 14,694 行** = 09-19（6,216）+ 09-20（8,418）+ 09-12（60）
  —— 正是停摆事故时段的首档补救。

**所以剩下的行动只有两条：T2、T3。**

---

## T2（⭐ 必须）：09-26（周六）08:00（北京）前必须在线

- 下一所内撮合窗口（`in_house`）= **09-26 08:00**，是**第 11 个样本外窗口**，
  同时是双机对照的机会。
- **上次的教训**（`data/b-side/说明.md` 原文）：窗口 08:00 开、乙侧 **23:14 才启动**
  → **白丢 15.2 小时**，不可回补。
- **要求**：**周五（09-25）晚启动**，周六 08:00 前**确认已在写入**。
- 自检方法：

```bash
python3 spread_sampler.py --once        # 跑一轮
ls -l data/b-side/spread/               # 当天文件应在增长
```

---

## T3（⭐⭐ 强烈建议）：这次起加采 5 档深度

**为什么**：目前 **5 档深度是单点故障** —— 全世界只有甲方在采 5 档，
所以甲方 6 处 / **1,642 分钟**的深度缺口**永久补不上**（乙侧现有数据覆盖率 0%）。
乙侧只要补一个文件，以后任何一方停摆，深度都有备份。

**做法**（`orderbook_sampler.py` 是**单文件、纯标准库、零依赖、无需 API Key**，已核实）：

```bash
# 拷 orderbook_sampler.py 一个文件即可（不需要整仓）
nohup python3 orderbook_sampler.py --loop > orderbook.log 2>&1 &
```

- 默认 30 秒一轮，每轮 10 配对 × 5 档 × 2 侧 ≈ 380 行，约 **50 MB/天**（原始 CSV；
  gzip 后约 5 MB，甲方仓库按日归档）。
- 交付命名沿用甲方口径：`data/b-side/spread/orderbook-YYYY-MM-DD.csv`。

---

## T4：交付未交的增量（09-21 起）

- 若 09-21 / 09-22 已采，按原有命名交付：`data/b-side/spread/YYYY-MM-DD.csv`
  （列与甲方 `spread_sampler.py` **逐字相同**）。
- 甲方验收三步（交付后甲方自己跑，乙侧可自检）：

```bash
python tools/audit_manifest_b.py        # ① 清单 vs 磁盘逐条对账（含 SHA256）
python tools/b_side_crosscheck.py --repo .   # ② 双机一致性（上次：572 组重叠，中位差 0.00 bp）
python tools/gap_capacity_report.py --write-supplement   # ③ 能补上多少 + 导出补充
```

---

## T5（不要做）：不要去补历史缺口

- 甲方 09-14 ~ 09-18 的 5 段 5 档缺口（合计 **907 分钟**），乙侧现有数据覆盖 **0%**，
  且**已不可回补**（当时乙侧没有在那个时段采样）。
- **不要重放、不要伪造** —— 覆盖率日报处置清单原话：
  **"若有不可恢复损失 → 在报告中如实标注该时段（不要伪造连续性）"**。
  历史缺口甲方已在报告中如实标注，不需要乙侧做任何事。

---

## 交付规范（沿用既有约定，`data/b-side/说明.md`）

1. **列与甲方 `spread_sampler.py` 逐字相同**：
   `ts_utc, ts_ms, date_cn, symbol, venue, base, bid, ask, mid, spread_bp, bid_sz, ask_sz, last, usdt_vol_24h`
2. **缺口按 `ts_ms` 跳过，不插值**。
3. 甲方合并时会**保留来源标记**（`source = b-side`）并**按轮去重**，
   不会把乙侧数据混进自有序列。
4. 交付方式：push 到 `origin/main` 的 `data/b-side/spread/`，或压缩包 + MANIFEST（含逐文件 SHA256）。

---

## 附：甲方当前 5 档缺口对照（2026-09-22 重算）

| # | 缺口时段（北京） | 时长 | ①甲方首档覆盖 | ②乙侧首档覆盖 |
|---|---|---|---|---|
| 1 | 09-20 02:48 → 15:03 | 735 分钟 | 0% | **100%**（乙侧 W2 已补上） |
| 2 | 09-16 18:19 → 09-17 02:20 | 481 分钟 | 1% | 0% |
| 3 | 09-15 03:51 → 08:13 | 262 分钟 | 1% | 0% |
| 4 | 09-17 16:36 → 17:56 | 80 分钟 | 2% | 0% |
| 5 | 09-18 18:21 → 19:33 | 72 分钟 | 1% | 0% |
| 6 | 09-14 10:24 → 10:36 | 12 分钟 | 23% | 0% |

> 复跑：`python tools/gap_capacity_report.py`
> （口径：分桶 1 分钟；缺口 = 相邻两轮间隔 > 10 分钟；**全局时间轴**，不是"文件内部"）
