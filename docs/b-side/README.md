# 乙侧投递物 · 导读与状态（b-side）

> **这一页是给甲侧（仓库主）看的。** 目的是让你**不必读完 `04`–`07`** 就知道：
> 哪些**已经闭环**、哪些**还开着**、这次**新增了什么**。
> 写于 **2026-09-13 01:00 CST**，核对基准：本仓库 `c30ce1f`。

---

## 一、目录里是什么

| 路径 | 内容 | 时点 |
|---|---|---|
| `docs/b-side/04`–`07` | 乙侧复核**原件，一字未改**（针对 `d11d19f`） | 09-12 22:30–23:30 |
| `docs/b-side/复核输出-20260912.txt` | `06` 全部数字的复算输出留档 | 09-12 23:20 |
| `docs/b-side/实测检验输出.txt` | 乙侧 6 标的折溢价 / 同步性 / fade / 流动性检验输出 | 09-11 |
| `docs/b-side/双机交叉验证-20260912.md` | **新增**：两台机器同时段盘口对照（492 组） | 09-13 00:45 |
| `data/b-side/raw/` | 6 标 rToken 1h 线 + 原生股日线（74 份原始 JSON） | 09-11 |
| `data/b-side/spread/` | **乙侧机器**采的逐分钟盘口（`base` 已规范化） | 09-12/13 |
| `tools/b_side_verify_panel.py` | 乙侧独立复算（读你的 `data/` 重算，不看结论） | 09-12 |
| `tools/b_side_checks.py` | 折溢价 / 同步性 / 隐含开盘 / fade / 流动性检验 | 09-11 |
| `tools/b_side_crosscheck.py` | **新增**：双机对照脚本 | 09-13 |
| `tools/b_side_normalize_base.py` | **新增**：`rstrip('USDT')` 残留清洗工具 | 09-13 |

> 两处约定：`04`–`07` 里出现的 `<工作台>` 是乙侧本机路径的**脱敏占位符**；文中的「`03` §x.x」指**你的 `docs/03`**。

---

## 二、已闭环（逐条对照你的 `08`/`09`/`11`，不必再管）

| 乙侧原条目 | 你的处置 | 状态 |
|---|---|---|
| 1️⃣ 基差符号两套约定 | `abb09a9` 统一为 `(永续/现货−1)`，5 处内联公式 + 方向映射 | ✅ 回归验证 0 不符 |
| 2️⃣ 日线 `--max-gap 3` 跨日拼接 | `abb09a9` 改按粒度自适应（日线 = 0） | ✅ 滞后>0 行 3,026 → **0** |
| 3️⃣ 日线 `closed` 实为周末 | `abb09a9` 新增 `bar_label` | ✅ |
| 4️⃣ 池内混商品 | `abb09a9`；你实测 **9 个**（比我们找到的 3 个更全） | ✅ |
| 5️⃣ F5 闸门须用可成交价 | **乙侧已在 `docs/11` 交付**（按你核实的费率 + 窗口重算） | ✅ |
| 采样"至少两个写入者" | `abb09a9` 自查确认（僵尸进程）并修复 | ✅ |
| Q3：请求复核 5min 序列 | **乙侧已在 `docs/11` 逐项复现** | ✅ |
| 你的反向反馈：深度口径统一 `min(bid,ask)` | 乙侧复现你的主张（中位 **$49**、`<$1000` **173/213**） | ✅ |
| 你的 Q2：本机采样请标注来源 | `data/b-side/spread/说明.md` 已标明**来源机器 = 乙侧本机** | ✅ |

---

## 三、还开着的（这才是现在要做的）

### A 侧（你的代码）—— 5 条

| # | 事项 | 现状（乙侧 09-13 00:40 全仓扫描） |
|---|---|---|
| 1 | `session_of()` 按平台口径分档 | ❌ **未做**：`build_panel.py` / `server/app.py` 里仍是美东时段判定，**全仓无 `in_house` / `stockroute`**（平台口径与你现口径差约 4 小时） |
| 2 | `cost_model` 按 `route` 分档 | ❌ 未实现 |
| 3 | 面板补 `B_mid` / `B_taker` / `B_maker` 三列 | ❌ 未做。`data/panel/*.csv` 现列：`basis_bp, spot_lag_bars, session, bar_label, pair_quality` |
| 4 | 美股节假日日历 | ❌ 无 |
| 5 | `docs/TASKS.md` 阶段 1 的 4 项 | ❌ 未勾：开盘深度复测 + 容量曲线、OQ-8、`DATA_DICT.md`、采样覆盖率日报 |

### B 侧（乙侧自己）—— 1 条属本次范围，其余按排期

| # | 事项 | 说明 |
|---|---|---|
| 6 | 策略名改为「**周末及节假日所内撮合做市**」（你 `09` §五.5 划给 B） | 依赖最终报告成稿，乙侧在报告里落这一条 |
| — | `docs/TASKS.md` 阶段 2/3/4 的 B 项 | 成本模型、逆向选择、maker 成交模型、参数冻结、样本外、6 段说明等 —— 按 `TASKS.md` 排期推进，不在本次投递范围 |

---

## 四、README 门户有 4 处数字与最新口径不一致（建议改）

| README 位置 | 现值 | 最新口径 | 依据 |
|---|---|---|---|
| 第 11 行、第 28 行 | 基差 **−10 ~ −29 bp（全为负）** | **正号**：1h 休市 **+12.47** / 盘中 **+10.35**；日线周末 **+8.18** / 工作日 **+7.08** | 你的 `08` §二.1（统一口径后全部翻号） |
| 第 29 行 | AR(1) **ρ = 0.533**，半衰期 ≈6 分钟 | 14 天全样本分时段：weekend **0.882** / intraday **0.670**；10 标的 ρ 中位 **0.8585** | 你的 `docs/10` ＋ 乙侧 `docs/11` |
| 第 46 行 | 永续顶档深度中位 **≈ $400** | `min(bid,ask)` 中位 **$49**（`<$1000` 的 173/213） | 你的 `08` §五.1 ＋ `docs/11` §D |
| 第 48 行 | 永续 1H 仅回溯 **约 41 天** | 1h 面板实测 **58 天**（2026-07-16 起） | 乙侧对 `data/panel/1h_10pairs.csv` 复算 |

> ⚠️ 第 46 行 **$400 与 $49 差 8 倍**，不是单纯"单侧 vs min"能解释的（那个差约 3 倍）。建议核对后再写进报告 —— 它直接决定「容量」这一节的结论。

---

## 五、数据卫生两则（顺手发现，均可用脚本复核）

**1. `data/spread/2026-09-12.csv` 里 `HOO`(644 行) 与 `HOOD`(84 行) 并存**

`280ba50` 修 bug **之前**的历史残留（`'RHOODUSDT'.rstrip('USDT')` 剥的是字符集，尾部 `T` 也被剥掉 → `HOO`）。
本项目提交的 `data/b-side/spread/` 已做同样规范化（由 `RHOODUSDT` 重建 base）。

```bash
python tools/b_side_normalize_base.py --repo . --dry-run   # 先看影响面
python tools/b_side_normalize_base.py --repo . --apply     # 真要改时
```

**2. `.gitignore` 尾部一段注释是 GBK 编码**

第 37 行（`chore: gitignore 忽略临时提交信息文件` 那次加的）在 UTF-8 下显示为 `# ??ʱ?ύ??Ϣ`。
Git 匹配不受影响，但编辑器/网页打开会乱码，建议整份重存为 UTF-8。

---

## 六、引用路径映射（`04`–`07` 原文 → 现在的位置）

| `04`–`07` 里写作 | 现在的位置 |
|---|---|
| `07-给甲侧的修正清单.md` | `docs/b-side/07-给甲侧的修正清单.md` |
| 工作台 `实测\` | `data/b-side/raw/`（74 份 JSON）＋ `tools/b_side_checks.py` |
| 工作台 `复核\` | `docs/b-side/复核输出-20260912.txt` ＋ `tools/b_side_verify_panel.py` |
| 工作台 `采样数据\` | `data/b-side/spread/` |

> 你 `docs/08` 开头写的「对象：`07-给甲侧的修正清单.md`」，现在指向 `docs/b-side/07-…`；不改也不影响阅读，若要改只需补目录前缀。

---

## 七、这批东西为什么**故意不包含**你的内容

避免两边各存一份、日后越走越偏：

| 不在本目录 | 为什么 |
|---|---|
| 你的全部代码与 `data/` | 本仓库就是权威 |
| `docs/00`–`11` | 你手上就是原文 |
| 官方《组员说明》等公开材料 | 双方都有 |
| 乙侧只读快照（含 `.git`） | 会造成嵌套仓库；用 `git clone` 代替 |
| 乙侧自己的独立仓库 `studying684/bitget` | 已弃用，成果全部并入本仓库（见 `data/b-side/spread/说明.md`） |
